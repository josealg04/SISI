import { FiltrotutorPipe } from './filtrotutor.pipe';

describe('FiltrotutorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrotutorPipe();
    expect(pipe).toBeTruthy();
  });
});
